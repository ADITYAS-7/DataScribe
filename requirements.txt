# Pinned to the exact versions verified locally (Python 3.12) — unpinned
# ranges previously let Streamlit Cloud pull an untested dependency combo
# that crashed the app at startup.
pandas==2.3.3
numpy==2.5.1
pyarrow==24.0.0
snowflake-connector-python[pandas]==4.6.0
openpyxl==3.1.5
streamlit==1.59.1
pydantic==2.13.4
python-dotenv==1.2.2
cryptography==49.0.0
PyYAML==6.0.3
pytest==9.1.1
